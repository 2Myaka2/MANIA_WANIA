# Stage 34.B.5 resume: external authority and downstream gates

The September 23, 2026 task supplies an external human RMSD assessment for the
accepted NAMD WT NaPi2b / 2SS / replica 1 five-frame pilot (0.1–0.5 ns):
`drift_detected=false`. The reviewer is Ramilya Akhmetovna; the exact supplied
name is retained in the evidence artifacts. This assessment does not cover the
full 100-ns trajectory or any other replica. No RMSD threshold or drift decision
is inferred by the helper.

The new authority also explicitly supplies `condition=PMm` for Egor's NAMD
source family. It does not apply to Alina's systems or unrelated inputs.
Condition, variant, disulfide state, engine and replica remain separate fields.
No filename-based condition rule is introduced.

The Dataset v1.0 external PBC preparation protocol is now scientifically
approved for GROMACS and NAMD: unwrap bonded fragments, center on protein,
then wrap complete bonded molecular fragments into the periodic box. Individual
atoms must not be wrapped independently. Each trajectory still requires its
applicable validation and evidence. MANIA internal MIC remains false.

This protocol approval supersedes the previous pending protocol decision; it
does not retroactively change historical diagnostics or validate all Dataset
trajectories. The existing `PbcAudit.scientific_pbc_status` is one fixed
historical field (`unresolved`), with no separate protocol approval field.
The new approval is therefore recorded in external provenance, without changing
the schema, old audit records, numerical cutoff cases, or preparation evidence.

`tools/stage34b5_resume_downstream_smoke.py` implements only the reached resume
path. It verifies the committed checkpoint and accepted B.3/B.4/RMSD archives,
hashes the prepared trajectory without opening coordinates, and binds the CSV,
PNG and method JSON without calculating RMSD again. Explicit authority is supplied
through `--authority-input`; no assessment defaults are provided.

`authoritative_rmsd_assessment.json` uses the existing strict
`ReplicaReviewQCEvidence` adapter and `RMSDDriftAssessment`. Its artifact evidence
contains the named human authority, exact scope, basis hashes and binding time.
The Stage 32.C assessment input is distinct from the Stage 32.D `manual_review`
decision-resolution type. Clean hard/review PASS produces an automatic available
decision under the accepted workflow; the human reviewer remains in the bound
assessment evidence, while the decision's manual-review fields remain null.

Before QC, the helper creates explicit condition-bound canonical copies. Complete
model equality after removing only the condition change proves that all
scientific fields remain identical to accepted source science. Original inputs
are preserved byte for byte. The aggregation template is a required technical
input to Stage 32, not a production availability decision. The actual Stage 31
manifest is generated by `run_dataset_qc`, and that exact QC output feeds
`run_replica_aggregation`. Its sources are frozen by path, SHA256 and row count
after QC and checked again after aggregation.

The independent n=1 check uses the frozen canonical source records directly,
without calling the production aggregator. It checks source denominators,
identity/window population, all exported aggregate fields, one contributing
replica, and null standard deviation. It does not substitute zero for missing
scientific evidence.

Specialised aggregation is not run: accepted B.4 partner identities are local
to this topology, and no authoritative aggregation correspondence is supplied.
Stage 31 supports protein-only input; Stage 33 supports empty specialised
aggregate tables for absent aggregate families. B.4 per-replica canonical science
is preserved and is not replaced with fabricated empty science.

The publication authority gate requires complete Stage 30
`DatasetSystemBiologicalAnnotations` with `annotation_scope=complete_for_system`.
No such input is bound by accepted B.3/B.4 evidence. Stage 33.D requires all 690
annotations for every historical system, even in a smoke. Its accepted
`build_dataset_release_residue_annotations` gate rejects missing authority.
Observed disulfide bonds and two glycan anchors do not authorize an exhaustive
system annotation declaration or all-false flags for unlisted sites.

The reached path therefore stops before Stage 33 publication. F1, F2,
cross-table validation and release validation receive explicit NOT-RUN records,
following the previous B.5 evidence convention. No publication metrics or final
Dataset release are claimed. The helper deliberately contains no unpublished
annotation inference or alternate publication schema. Completing this next gate
requires authoritative complete system annotations and explicit publication
controls in a subsequent task.

Run the focused tests with:

```bash
.venv/bin/pytest -q tests/test_stage34b5_resume_downstream_smoke.py
```

The tests use synthetic fixtures only. They exercise human authority transport,
scope, evidence tampering, condition isolation, PBC history preservation,
accepted QC decisions, n=1 independent verification, the annotation stop, and
the existing F1/F2 protections. Regression success is not a real release PASS.
No production modules, scientific formulas, dependencies or public CLI change.
Stage 34 remains incomplete; the real three-replica chain still needs replicas
2/3 and its own authoritative QC and downstream evidence.

## Publication-only resume after external annotation authority

The subsequent September 23 task supplies complete system annotations for the
same NAMD WT / 2SS / PMm / replica-1 pilot: glycosylation `[295, 308]`, disulfide
variant sites `[303, 322, 328, 350]`, and cysteine variant sites `[]`.
`tools/stage34b5_publication_resume.py` strictly reads the existing
`DatasetSystemBiologicalAnnotations` artifact and a separate provenance record.
The accepted annotation schema has only Dataset/system identity fields; the
sidecar binds the full trajectory identity, PMm condition, canonical reference,
request evidence, and provenance for every category, including the empty list.
No fields are added to the annotation or release schemas.

Egor's supplied glycosylation and 2SS declarations are the biological authority.
Previously recorded PSF bonds are independent corroboration, never the source
of a completeness claim. The accepted explicit WT mapping supplies the sequence
evidence for the externally accepted empty cysteine list. The helper checks
ASN295/ASN308 and CYS303/CYS322/CYS328/CYS350 against the pinned reference.
All 690 published annotations must preserve complete negative semantics, ECD
234–361, and MX35 311–341.

The publication helper verifies the previous resume against its archived
inventory and pins the QC-derived manifest, original B.3 canonical source,
condition-bound Stage 31 source, QC decision, and aggregate hashes. It calls
only the accepted upstream integrity/lineage readers and the independent n=1
comparison. It does not run QC, regenerate a decision, aggregate, read trajectory
coordinates, or calculate RMSD. The protein publication binding resolves to the
same Stage 31 canonical file. Per-replica lipid/glycan sources are retained;
their absent aggregate families use Stage 33's accepted header-only behavior.

The historical temporal artifact still carries `condition=null` and execution
label `namd-pilot`. An additive publication copy binds both condition labels to
the previously accepted PMm authority. Complete equality after reverting those
two labels proves that the sampling/window plans and scientific interval remain
unchanged. The historical artifact and all frozen Stage 32/31 inputs stay intact.

Contact definitions come from recorded B.3 options and edge semantics plus the
accepted Stage 28/29 contracts. Optional metrics remain empty because this smoke
has no separately supplied metric selection; canonical science remains in its
normal tables. F1 compares complete canonical models; F2 runs with zero optional
metric records. No fixture or unsupported analysis metric is inserted.

The helper runs the existing Stage 33 workflow, both corrective guards, all-table
validation, and complete offline release reconstruction. Independent checks read
the emitted science/aggregate CSVs and compare every retained source field.
Any authority or validation failure records STOP and prevents a B.5 PASS claim.
The evidence keeps the external PBC approval separately from historical
`scientific_pbc_status=unresolved` diagnostics, with internal MIC false.

This remains a single-replica 0.1–0.5 ns publication smoke. The frozen release
version identifies the publication contract; it does not declare final Dataset
v1.0, a 100-ns analysis, or three-replica production. Stage 34.C still requires
real replicas 2 and 3 and authoritative specialised partner correspondence.

Required explicit controls are `--prior-evidence`, `--annotation-input`,
`--annotation-provenance`, and `--publication-inputs`. The helper creates a unique
ignored evidence directory; its linked historical inputs must remain available
for reconstruction. The task evidence archive includes controls, checks, actual
publication outputs, and reference records for the historical archives.

Focused verification:

```bash
.venv/bin/pytest -q tests/test_stage34b5_publication_resume.py
.venv/bin/pytest -q tests/test_dataset_release_source_authority.py
```
